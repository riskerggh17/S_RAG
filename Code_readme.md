# LLMops 企业级运维与部署 RAG 专家助手 - 开发文档

## 项目概述

本项目旨在构建一个企业级的运维与部署专家助手，利用检索增强生成（RAG）技术，为运维人员提供精准、可靠的技术支持。该助手将整合 Docker、Linux 等多种技术文档，通过大语言模型（LLM）生成基于事实的回答，解决传统 LLM 知识更新不及时和产生“幻觉”的问题。

**项目名称:** LLMops 企业级运维与部署 RAG 专家助手

**核心技术栈:**
*   **大语言模型 (LLM):** 用于理解和生成自然语言。
*   **检索增强生成 (RAG):** 结合外部知识库，提升 LLM 回答的准确性和可信度。
*   **向量数据库 (Vector DB):** 存储和高效检索文档的向量表示。
*   **嵌入模型 (Embedding Model):** `nomic-embed-text-v2-moe`，用于将文本转换为向量。

## 系统架构

整个系统遵循经典的 RAG 架构，分为数据准备、索引、检索和生成四个核心阶段。

1.  **数据准备 (Data Preparation):** 从多种来源（如 Markdown 文档、Linux 手册）收集原始数据，并进行清洗和标准化。
2.  **索引 (Indexing):** 将清洗后的文本切分成合适的块（Chunk），使用嵌入模型将其转换为向量，并连同元数据一起存入向量数据库。
3.  **检索 (Retrieval):** 接收用户查询，通过混合检索策略（元数据过滤 + 向量/关键词搜索）从向量库中找出最相关的知识块。
4.  **生成 (Generation):** 将检索到的知识块作为上下文，与用户问题一同提交给大语言模型，生成最终答案。

## 数据管道

数据管道是 RAG 系统的基石，负责将非结构化的原始文档转化为可供检索的高质量数据。

### 数据清洗

清洗的目标是移除干扰信息、标准化格式，并为后续的切片做好准备。我们为不同类型的数据设计了专门的清洗器。

#### Docker 文档清洗 (`cleaners/clean_get_started.py`)

针对 `get-started` 等 Markdown 格式的文档，清洗逻辑如下：

1.  **移除图片:** 使用正则表达式 `!$$[^$$]*$$$[^$]+$` 删除所有图片引用，避免无效信息干扰。
2.  **简化链接:** 将 Markdown 链接 `[文本](URL)` 替换为纯文本 `[文本]`，保留语义的同时移除冗余 URL。
3.  **清除 HTML 标签:** 使用正则 `<[^>]+>` 移除所有 HTML 标签。
4.  **代码块保护:** 这是核心步骤。使用正则 ` ```(\w*)\n([\s\S]*?)\n``` ` 识别代码块，并用特殊的分隔符包裹，标记其为不可分割的整体。
    *   **标记前:**
        ```python
        def hello():
            print("world")
        ```
    *   **标记后:**
        ```text
        ###CODE_BLOCK_START###|lang=python
        def hello():
            print("world")
        ###CODE_BLOCK_END###
        ```
5.  **输出:** 将清洗后的文本与元数据（`id`, `source`, `category`, `path`）一同保存为 JSON 文件。

#### Linux 手册清洗 (`cleaners/clean_linux_man.py`)

针对 Linux `man` 手册页，清洗逻辑如下：

1.  **去除控制字符:** 移除 `man` 命令特有的退格符和加粗控制符（如 `.\x08`）。
2.  **格式化:** 将纯文本内容整理为清晰的段落。
3.  **输出:** 同样输出为带有 `source: "linux"` 和 `category: "linux_manual"` 标签的 JSON 文件。

### 文本切片

切片的目标是将长文本分割成适合嵌入模型处理的小块，同时保持语义的完整性。

*   **模型限制:** 嵌入模型 `nomic-embed-text-v2-moe` 的上下文窗口为 512 个 Token。
*   **切片策略:**
    1.  **代码块保护:** 切片器会首先识别 `###CODE_BLOCK_START###` 标记。如果一个代码块的长度在模型限制内，它将作为一个独立的切片（Chunk）被完整保留，绝不会被拦腰截断。
    2.  **普通文本:** 对于非代码文本，采用递归字符切片。
        *   **块大小 (Chunk Size):** 设置为 400-450 Token，为 Tokenizer 预留缓冲空间。
        *   **重叠 (Chunk Overlap):** 设置为 50 Token，确保上下文连贯，避免信息在切分点丢失。
        *   **分隔符:** 优先按 `["\n\n", "\n", " ", ""]` 等自然边界进行切分。

### 元数据设计

元数据是实现高级检索和溯源的关键。每个切片都会附带以下元数据：

| 字段名     | 示例值                            | 用途                                                         |
| :--------- | :-------------------------------- | :----------------------------------------------------------- |
| `source`   | `"docker"`, `"linux"`             | **核心过滤器**。用于在检索时限定搜索范围，例如只搜索 Docker 相关文档。 |
| `category` | `"get-started"`, `"linux_manual"` | **分类标签**。可用于调整检索权重或进行更细粒度的过滤。       |
| `path`     | `"docs/get-started/overview.md"`  | **溯源**。在 AI 回答后，用于生成指向原始文档的链接。         |
| `id`       | `"doc_1_chunk_5"`                 | **唯一标识**。用于数据更新和去重。                           |

## 检索与生成策略

### 混合检索策略

为了兼顾检索的精准度和召回率，我们采用“元数据过滤 + 混合检索”的两层架构，而非串行的多层检索。

1.  **第一层：元数据过滤 (Metadata Filtering)**
    *   首先通过意图识别（可由 LLM 完成）判断用户查询的领域。
    *   例如，用户提问“Docker 如何重启？”，系统会生成一个过滤条件 `source == 'docker'`。
    *   这一步能迅速将搜索范围从整个知识库缩小到特定领域，排除无关干扰，极大提升效率和准确性。

2.  **第二层：混合检索 (Hybrid Search)**
    *   在过滤后的数据范围内，同时进行两种检索：
        *   **向量检索 (Semantic Search):** 将用户问题转化为向量，寻找语义上最相似的文档块。这解决了“词不同但意同”的问题（如“容器启动” vs “run command”）。
        *   **关键词检索 (Keyword Search / BM25):** 寻找包含用户查询中特定关键词的文档块。这确保了对专有名词、命令、错误码的精确匹配。
    *   **结果融合 (RRF):** 使用倒数融合（Reciprocal Rank Fusion, RRF）等算法，将两路检索的结果进行合并和重排序，最终选取排名最高的几个切片作为上下文。

### 生成

将用户原始问题和检索到的 Top-K 个文档切片，按照预设的提示词（Prompt）模板进行组装，然后发送给大语言模型。

**提示词模板示例:**

```
你是一个专业的运维专家助手。请根据以下检索到的上下文信息来回答问题。如果无法从上下文中找到答案，请回答“根据现有资料，我无法回答这个问题”。

**上下文信息:**
{retrieved_chunks}

**用户问题:**
{user_question}

**回答:**
```

## 部署与运维 (LLMOps)

上线并非终点，而是持续运营的起点。为确保助手稳定、高效、安全地运行，需遵循以下 LLMOps 实践。

### 生产上线清单

1.  **密钥管理:** API Key、数据库密码等敏感信息必须通过环境变量或专业的密钥管理服务注入，严禁硬编码在代码中。
2.  **审计与权限:** 建立清晰的权限体系，明确谁可以修改提示词、更新知识库或查看运行日志。所有关键操作都应记录在审计日志中。
3.  **可观测性:**
    *   **性能监控:** 监控 API 的响应延迟、错误率、吞吐量等。
    *   **成本监控:** 追踪 LLM 的 Token 消耗和向量数据库的存储成本。
    *   **质量监控:** 收集用户的“赞/踩”反馈，定期评估回答的准确性和相关性。
4.  **数据合规:** 确保输入和输出的数据不包含敏感信息，或已进行脱敏处理。制定明确的数据留存和删除策略。

### 持续优化

1.  **反馈闭环:** 将用户的负面反馈（如“踩”）和人工修正的答案收集起来，形成一个高质量的评估数据集。
2.  **评估与迭代:** 定期使用评估数据集对系统进行测试，衡量检索的召回率和最终回答的质量。根据评估结果，持续优化数据清洗规则、切片策略、检索参数和提示词。
3.  **版本控制:** 对提示词、数据管道代码和模型配置进行版本控制（如使用 Git），确保任何变更都可追溯、可回滚。



